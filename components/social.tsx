import {Mesh, Vector3} from "three";
import {extend} from "@react-three/fiber";

extend({Mesh})

const Social = (path: string, href: string, position: Vector3, rotation: Vector3) => {

    return (
        <mesh receiveShadow={true}>

        </mesh>
    )
}

export default Social